# Інтелектуальна вебсистема автоматизованого підбору конфігурації персонального комп'ютера на основі потреб користувача

## Опис проєкту
Цей проєкт є демонстраційним прототипом вебсистеми, яка автоматично підбирає конфігурацію персонального комп'ютера відповідно до потреб користувача.

Система дозволяє:
- визначити сценарій використання ПК вручну або через локальну ML-модель;
- отримати основну рекомендовану конфігурацію;
- переглянути альтернативні варіанти збірки;
- побачити пояснення щодо вибору комплектуючих;
- побачити не лише ціну, а й джерело ціни, магазин, посилання та наявність товару;
- зберегти збірку в поточному профілі;
- переглянути історію запитів і повторно відкрити попередні результати.

Проєкт розроблено в межах бакалаврської роботи за темою:
**«Інтелектуальна вебсистема автоматизованого підбору конфігурації персонального комп'ютера на основі потреб користувача»**.

---

## Основні можливості
- вебінтерфейс для підбору конфігурації ПК;
- підтримка сценаріїв:
  - ігровий ПК;
  - офісний ПК;
  - ПК для навчання;
  - ПК для монтажу / 3D;
- локальне AI/ML-визначення призначення ПК на основі текстового опису;
- автоматичний та ручний режим підбору бюджету;
- формування основної конфігурації та альтернатив для порівняння;
- перевірка сумісності компонентів;
- пояснення вибору комплектуючих;
- підтримка профілів користувача;
- історія запитів;
- збережені збірки;
- автоматичне оновлення цін з зовнішніх джерел;
- відображення ринкової інформації по комплектуючих:
  - магазин;
  - джерело ціни;
  - наявність;
  - час останнього оновлення;
  - посилання на товар;
- пріоритезація комплектуючих, що є в наявності;
- логування ключових подій системи;
- unit-тести для основних і проблемних сценаріїв.

---

## Використані технології
- **Python**
- **FastAPI**
- **Jinja2**
- **Uvicorn**
- **Pytest**
- **scikit-learn / joblib** для локальної ML-моделі
- **Playwright** для оновлення цін
- **HTML / CSS / JavaScript**

---

## Структура проєкту
```text
.
├── app.py                         # Точка входу FastAPI-застосунку
├── builder.py                     # Публічний фасад для builder_engine
├── build_service.py               # Compatibility-wrapper для services.build_service
├── common.py                      # Compatibility-wrapper для builder_engine.common
├── builder_engine/                # Основна логіка підбору ПК
│   ├── core.py                    # Головний вхід у побудову конфігурації
│   ├── scenarios.py               # Сценарії gaming/office/study/creator
│   ├── recommendations.py         # Автоматичний підбір бюджету й альтернатив
│   ├── explanations.py            # Пояснення щодо вибору комплектуючих
│   └── common.py                  # Спільні функції builder-рівня
├── config.py                      # Конфігурація застосунку та бюджетні межі
├── parts_db.py                    # База комплектуючих і сценаріїв використання
├── routes/                        # HTTP-маршрути
│   ├── web.py                     # Основні сторінки та побудова конфігурації
│   └── saved_builds.py            # Профілі, історія, збережені збірки
├── services/                      # Сервісний шар
│   ├── build_service.py           # Нормалізація форми та підготовка результату
│   └── ai_service.py              # Робота з локальною AI/ML-моделлю
├── repositories/                  # Робота з JSON-сховищами
├── schemas/                       # Pydantic-схеми
├── utils/                         # Допоміжні функції для підготовки даних до UI
├── ml/                            # Локальна ML-модель, навчання та прогнозування
├── templates/                     # HTML-шаблони
├── static/                        # Статичні ресурси
├── data/                          # Дані для оновлення цін
├── scripts/                       # Допоміжні скрипти
├── tests/                         # Unit-тести
├── saved_builds.json              # Збережені збірки
└── user_profiles.json             # Профілі та історія запитів
```

---

## Як працює система

### 1. Вибір сценарію використання
Користувач обирає тип ПК або вводить текстовий опис своїх потреб. Якщо використовується текстовий опис, локальна ML-модель прогнозує найбільш імовірний сценарій використання.

### 2. Введення параметрів
Після вибору сценарію користувач задає бюджет, пріоритет підбору та додаткові параметри, наприклад:
- роздільну здатність;
- бажаний FPS;
- програми;
- кількість вкладок;
- кількість моніторів;
- обсяг RAM та SSD;
- тип графіки;
- наявність Wi-Fi.

### 3. Побудова конфігурації
Сервісний шар нормалізує вхідні параметри та передає їх у builder, який:
- визначає вимоги до збірки;
- обирає сумісні комплектуючі;
- перевіряє бюджетні обмеження;
- намагається віддати пріоритет компонентам, що є в наявності;
- формує основну рекомендацію;
- формує альтернативні конфігурації.

### 4. Формування результату
Система показує:
- назву сценарію;
- рівень збірки;
- загальну вартість;
- усі комплектуючі;
- пояснення до кожного компонента;
- примітки щодо сценарію та бюджету;
- ринкову інформацію по кожному компоненту, якщо вона доступна.

### 5. Збереження та історія
Користувач може:
- зберегти збірку;
- переглянути попередні результати;
- повторно відкрити збірку з історії;
- видалити окремий запис історії;
- очистити всю історію.

---

## AI/ML-модуль
Локальний AI-модуль використовується для автоматичного визначення призначення ПК на основі текстового опису користувача.

Підтримувані класи:
- `gaming`
- `office`
- `study`
- `creator`

Особливості:
- модель працює локально;
- зовнішні AI API не використовуються;
- прогноз приймається за порогом впевненості;
- якщо впевненість недостатня, користувач може обрати сценарій вручну.

---

## Актуалізація цін і ринкових даних
Технічні характеристики компонентів зберігаються локально у `parts_db.py`, а ціни та ринкові метадані можуть оновлюватися автоматично через скрипти в папці `scripts/`.

Основні файли:
- `data/price_links.json` — список джерел для комплектуючих;
- `data/price_feed.json` — збережений актуальний набір цін і метаданих;
- `scripts/refresh_prices.py` — скрипт оновлення цін.

У `price_feed.json` для компонента можуть зберігатися:
- `price`;
- `source_used`;
- `store`;
- `availability`;
- `in_stock`;
- `checked_at`;
- `product_url`.

Під час побудови конфігурації система підтягує ці поля в `Part.meta`, а потім відображає їх у результаті збірки.

### Приклад запуску оновлення цін
```powershell
python scripts/refresh_prices.py
```

---

## Запуск проєкту

### 1. Активувати віртуальне середовище
```powershell
.venv\Scripts\activate
```

### 2. Встановити залежності
```powershell
python -m pip install -r requirements.txt
```

### 3. Запустити застосунок
```powershell
python -m uvicorn app:app --host 127.0.0.1 --port 8010
```

Після запуску застосунок буде доступний у браузері за адресою:
```text
http://127.0.0.1:8010
```

---

## Запуск тестів
```powershell
python -m pytest
```

На поточному етапі в проєкті є unit-тести для:
- AI-модуля;
- builder-рівня;
- build service;
- маршрутів;
- історії та збережених збірок;
- актуалізації ринкових метаданих;
- логування та edge cases.

---

## Логування
У проєкті реалізовано логування ключових подій:
- запуск застосунку;
- AI-визначення сценарію;
- старт побудови конфігурації;
- fallback-підбір окремих компонентів;
- помилки валідації форми;
- робота з профілями;
- збереження збірок;
- історія запитів;
- очищення або видалення записів;
- оновлення цін.

Логування використовується для:
- діагностики проблем під час розробки;
- перевірки сценаріїв побудови;
- контролю коректності fallback-логіки;
- аналізу причин невдалої побудови конфігурації.

---

## Реалізований функціонал у межах дипломної роботи
У коді реалізовано:
- інтелектуальний аналіз потреб користувача;
- автоматизований підбір конфігурації ПК;
- перевірку сумісності компонентів;
- формування альтернативних конфігурацій;
- пояснення вибору комплектуючих;
- підтримку профілю користувача;
- збереження збірок;
- історію запитів;
- актуалізацію цін;
- врахування наявності комплектуючих;
- відображення джерела ціни та магазину;
- unit-тести;
- логування основних подій системи.

---

## Призначення проєкту
Проєкт орієнтований на демонстрацію підходу до автоматизованого підбору конфігурацій ПК на основі потреб користувача та може використовуватися як демонстраційний програмний прототип у межах бакалаврської роботи.
